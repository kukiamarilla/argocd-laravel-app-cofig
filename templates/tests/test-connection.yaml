apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argocd-laravel-app-config.fullname" . }}-test-connection"
  labels:
    {{- include "argocd-laravel-app-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argocd-laravel-app-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
